# Numbers that may appear in checked documentation without being present in results/ or runs/.
# One number per line; everything after '#' is the reason.
0        # labels / percentages
1        # labels, section numbers, "1 file"
2        # depth-2 tree, two clauses, two fields
3        # three runs, three seeds, three repeats
4        # per-device batch size (also in training_config.json)
5        # five folds
8        # max_new_tokens (also in eval.json decoding)
10       # ten Asset_ID trucks
12       # non-rule feature columns after dropping Timestamp
13       # prompt lines left after removing the two rule fields
14       # prompt lines left after removing one rule field
15       # input columns
16       # CSV columns; effective batch size
20       # eval subset rows (10% of 200)
50       # first step with zero training loss (also from trainer_state summary)
80       # 80/20 split
95       # 95% intervals
100      # percentages; 100-epoch run
9        # "9 月" (September) in the Chinese summary
1.15     # scheduler overshoot factor inherited from upstream
1.24     # 1,235,814,400 parameters rounded to billions
1.2      # ditto
2024     # year
2025     # year
2026     # year
3.2      # Llama 3.2
3.1      # Llama 3 chat template family mention
3.10     # Minimum Python version required by pyproject.toml (requires-python >=3.10)
4.55.4   # transformers version
128004   # <|finetune_right_pad_id|>
128256   # vocabulary size
0.6      # sampling temperature in the original generation_config
0.9      # top_p in the original generation_config
0.1      # weight decay
0.05     # alpha for 95% CI
90       # latitude bound
180      # longitude bound
1.b      # license clause
i        # license clause
iii      # license clause
5070     # GPU name (RTX 5070 Ti)
512      # max_new_tokens of the legacy Flask app
6        # section numbers / list positions
7        # section numbers / list positions
1300     # earliest surviving checkpoint step of run sc904
128001   # Instruct eos_token_id list
128008   # Instruct eos_token_id list
128009   # Instruct eos_token_id list
12.4     # 1,235,814,400 parameters in 亿 (Chinese case study title)
1.235    # ditto, in billions with three decimals
2016     # Olist data years
2017     # Olist data years
2018     # Olist data years / split date
1.7      # version prefix of scikit-learn 1.7.2
4.0      # CC BY-NC-SA 4.0
-90      # latitude bound
-180     # longitude bound
# --- counts that no result file stores directly are derived by the script itself
# (probe_set_counts from results/eval.json, test functions from tests/), so they are not listed here.
1090     # seconds between the two TensorBoard event files of run sc904 (runs/sc904/tensorboard_events.json)
18       # the same interval in minutes
1725     # 1500 * 1.15 planned scheduler steps (the factor and the step count are both in runs/)
106      # CSV size in KB as displayed on the Kaggle page
177      # distinct test rows edited by the four rule-field probes (50 + 43 + 84)
2018     # Olist split year
105482   # size in bytes of the committed data/smart_logistics_dataset.csv (LF)
24       # 12 control words x 2 rule fields = the control probe sets (CONTROL_WORDS in src/delaysentinel/probes.py)
12       # control words
5e-5     # the rounding boundary of the 4-decimal training log, quoted in runs/RUNS.md as an explanation, not as a result
1e-6     # the unit of the eval-loss column in runs/RUNS.md and README.md, not a value
555      # 38,257 - 37,702: the test-window orders the in-flight sensitivity run adds (stated in the note field of results/olist_positive_control.json)
